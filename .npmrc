# Pin this project to the public npm registry so it never inherits a private
# default registry from the user-level ~/.npmrc (e.g. a corporate Artifactory).
# This keeps the lockfile portable and lets CI install without private creds.
registry=https://registry.npmjs.org/

enable-pre-post-scripts=true
